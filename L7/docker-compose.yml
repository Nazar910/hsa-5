version: "3.9"
services:
  static-server:
    build: ./staticServer
    volumes:
      - ./static:/images
    ports:
      - 8000:8000
  nginx:
    image: "nginx:1.21.4"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./data:/data
    ports:
      - 8080:8080
